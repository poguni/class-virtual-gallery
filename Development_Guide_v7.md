# 우리 반 가상 미술관 (Class Virtual Gallery) — 개발 가이드 v7 (통합 최종 보고서)

**작성일** 2026-07-24
**이 문서의 성격** Phase 1부터 지금까지 진행된 모든 작업(다중 전시실 & 순환 동선 → QR코드 &
배포 → 로비 디자인 개선 → 모바일 UX 개선 & 감상 결과 리포트)을 하나로 통합한 최종 보고서입니다.
기존 `Development_Guide_v5.md`, `v6.md`의 내용을 모두 포함하며, 이후 새 대화창에서는 이 문서
(v7) 하나만 참고하면 됩니다. 나중에 새 Phase를 이어서 진행할 때도 이 문서에 이어 붙여
`v8`로 갱신하는 방식을 유지해 주세요.

---

## 0. 새 대화창에서 작업을 이어가는 방법

1. 이 문서(`Development_Guide_v7.md` 또는 `.html`)
2. 현재까지의 프로토타입 파일 `index.html`
3. `assets/gallery_background_pc.jpg`, `assets/gallery_background_mobile.jpg`
4. (선택) `가상미술관_PRD.md`, `README.md`(배포 가이드)

> "첨부한 Development_Guide_v7과 index.html을 참고해서 이어서 개발해줘. 다음 작업은 Phase 7(◯◯)이야."

---

## 1. 프로젝트 개요

학생들이 미술 시간에 완성한 작품을 사진으로 찍어 올리면, Three.js 기반 3D 가상 미술관으로
자동 전시되는 초등학교 학급용 웹 앱입니다. Vanilla JS + CDN 라이브러리 + Supabase(DB·Storage)
+ GitHub Pages 조합으로, 별도 빌드 과정 없이 파일을 그대로 올리면 동작하도록 만들어졌습니다.
자세한 기획 배경·타겟 사용자·기능 목록은 `가상미술관_PRD.md` 참고.

---

## 2. 전체 진행 이력 (한눈에 보기)

| Phase | 내용 | 상태 |
|---|---|---|
| 1~3 | 전시 자동 생성 · 1인칭 관람(WASD+마우스/조이스틱+드래그) · 좋아요·댓글 · 관리자 업로드/관리 페이지 | ✅ 완료 (이전 세션) |
| 4 | 다중 전시실 & 순환 동선(ㅁ자 구조), 조명 성능 최적화 | ✅ 완료 |
| 5 | QR코드(시작 화면 자동 표시) & GitHub Pages 배포 가이드 | ✅ 완료 |
| 5.5 | 로비 디자인 개선 (코브 라이트 · 바닥 메달리온 · 브라스 라인) | ✅ 완료 |
| 6 | 모바일 UX 안정화(조이스틱 자체 구현) & 감상 결과 리포트 다운로드 | ✅ 완료 |
| 6-보류 | 성능 옵션(지오메트리 병합) | ⏸ 보류 — 방별 디자인 커스터마이징과 상충 가능성 있어 사용자가 명시적으로 보류 |
| 6-보류 | 댓글 부적절어 필터 | ⏸ 보류 — 사용자가 명시적으로 보류 |
| 7 | (다음 작업 후보 — 8번 로드맵 참고) | 예정 |

---

## 3. 전체 공간(동선) 구조 — Phase 4

```
                 ┌───────────────┐
                 │     로비      │  (환영 사인: 실제 전시 학년/반 표시, 코브 라이트·바닥 메달리온·브라스 도어 라인)
                 │  A동문   B동문 │
                 └──┬─────────┬──┘
                    │         │
              [A동 복도]   [B동 복도]
                    │         │
              [제1전시실]  [제N전시실] (B동은 방 번호를 뒤집어서 지음)
                    │         │
                    …         …
                    │         │
              [제k전시실]  [제k+1전시실]
                    │         │
                    └────┬────┘
                   [연결 통로(가로)]  ← A동·B동의 가장 안쪽 끝을 이어붙임
```

- **A동(나가는 길)**: 전체 전시실의 앞쪽 절반. 로비 왼쪽 문에서 시작해 안쪽으로 들어감.
- **B동(돌아오는 길)**: 전체 전시실의 뒤쪽 절반. **방 순서를 뒤집어서** 지어놨기 때문에, 연결
  통로를 지나 로비 쪽으로 걸어오면 원래 번호 순서(예: 3관 → 4관) 그대로 마주치게 됨.
- **연결 통로**: 두 동의 가장 깊숙한 끝을 가로로 이어붙여, 로비 → A동 → 연결 통로 → B동 → 로비로
  돌아오는 완전한 순환 경로를 만듦.
- 전시실이 1개뿐이면 B동에는 배정할 방이 없는데, 이 경우에도 통로만 있는 짧은 복도를 만들어
  순환 구조 자체는 항상 유지됨.
- **로비만** 별도로 격을 높인 디자인 디테일(코브 라이트·바닥 메달리온·브라스 도어 라인)이
  적용되어 있고, 전시실은 의도적으로 차분한 무채색 톤을 그대로 유지함 (5.5절 참고).

---

## 4. 코드 구조 — 핵심 함수·상수 레퍼런스

> 파일은 하나(`index.html`)이며, 두 개의 `<script>` 블록으로 구성됨: 첫 번째는 로그인/인증/
> QR코드/시작 화면 로직, 두 번째는 3D 씬 · 관리자 페이지 · 감상 결과 리포트 전체.

### 4.1 공간 치수 상수
| 이름 | 의미 |
|---|---|
| `LOBBY` | 로비 크기(고정, `{minX:-6,maxX:6,minZ:-1,maxZ:6,h:5}`) |
| `CORR_MINX/MAXX`, `CORR_HALF`, `CORR_WIDTH` | 복도·문 폭 (2.8 unit) |
| `CORR_LEN` | 방과 방 사이 복도 길이 (6 unit) |
| `ROOM_H` | 전시실 높이 (5 unit, 모든 방 공통) |
| `ROOM_FRONT_MARGIN`, `ROOM_BACK_MARGIN`, `ARTWORK_STEP` | 전시실 안 작품 배치 간격 계산에 씀 |
| `WING_CENTER_A`(-4), `WING_CENTER_B`(4) | A동·B동 복도의 중심 x좌표 |
| `WING_ROOM_OUT` | 전시실이 복도에서 바깥쪽으로 뻗는 폭 (10 unit) |
| `DOOR_H` | 문틀(아치) 높이 (4.0 unit) |

### 4.2 동선/방 생성 함수 (Phase 4)
- **`computeRoomDepth(count)`** — 작품 수에 맞춰 전시실 깊이 계산 (좌우 분할, 4 unit 간격 유지)
- **`buildRoomWalls(room, centerX)`** — 전시실 4면 벽. 앞/뒤 모두 문이 열려 있음(순환 구조라 막다른 방이 없음)
- **`buildCorridorWalls(corr)`** — 복도 좌우 벽만 세움
- **`buildDoorFrame(centerX, z, doorWidth, accent)`** — 기둥 2개 + 상인방으로 이루어진 문틀.
  `accent=true`(로비 출입문 전용)면 얇은 브라스 라인 트림을 추가로 덧댐 (4.4절 참고)
- **`addCorridorLight(corr)`** — 복도 한가운데 쿨톤 보조등
- **`addRoomAccent(room, colorHex, outward)`** — 전시실 입구 안쪽에 방마다 다른 색 세로 스트라이프
- **`buildWingStep(...)` / `buildWing(...)`** — 한 동에 배정된 방들을 순서대로 복도+전시실로 지음
- **`extendStraightCorridor(centerX, fromZ, toZ)`** — 두 동의 깊이가 다를 때 얕은 쪽을 곧은 복도로 이어 붙임
- **`buildConnector(farZ)`** — 두 동의 가장 안쪽 끝을 가로로 이어붙이는 연결 통로
- **`buildRoomsAndCorridors(roomsData)`** — 위 함수들을 조합해 로비 뒤 전체 순환 동선을 만들고
  이동 충돌에 쓰이는 `SEGMENTS` 배열을 채움 (최상위 진입점)
- **`addRoomSign(...)` / `addLobbyDoorSign(...)` / `buildLobbySign(...)`** — 전시실 입구 안내판,
  로비 출입문 안내판, 로비 환영 사인(실제 전시 학년/반 반영)

### 4.3 이동 충돌 시스템 (Phase 4)
- **`SEGMENTS`** — 로비/복도/전시실/연결통로를 사각형으로 표현한 배열
- **`findSegment(x, z)`** — 현재 좌표가 어느 세그먼트 안에 있는지 판정 (복도/연결통로 우선 확인 → 전시실)
- **`clampPlayerPosition(pos)`** — 세그먼트별로 진행 방향 축은 자유, 폭 방향 축만 벽 안으로 제한

### 4.4 로비 전용 디자인 디테일 (Phase 5.5, 신규)
> 전시실은 그대로 무채색을 유지하고, 로비만 "감상 전 전환 공간"으로서 격을 높이는 방향으로
> 설계함. 세 요소 모두 브라스 톤(`0xcaa15a`/`0xe0bd7a` 계열)을 공유해 통일감을 줌.

- **`lobbyBrassMat`** — 로비 전용 광택 브라스 재질(`doorFrameMat`보다 밝고 금속성 높음)
- **`buildDoorFrame(...)`의 `accent` 파라미터** — 로비의 두 출입문(`WING_CENTER_A`/`WING_CENTER_B`
  호출부만 `true`로 전달)에 얇은 세로 브라스 라인 2줄 + 상인방 아래 가로 라인 추가
- **`coveLightMat` / `addLobbyCoveLight(x1,z1,x2,z2,nx,nz)`** — 로비 천장-벽 경계 5구간(좌·우측
  벽, 뒷벽 3조각)에 두르는 웜톤 emissive 라인. 실제 광원이 아니라 발광 재질이라 조명 개수를
  늘리지 않아 성능에 영향 없음
- **`makeLobbyMedallionTexture()` / `lobbyMedallionMat` / `lobbyMedallion`** — 로비 바닥 중앙에
  캔버스로 그린 원형 인레이 패턴(동심원 + 브라스 링 + 방사형 라인). 기존 바닥 팔레트 톤 안에서
  명암 대비만 주는 방식이라 색이 늘지 않음. `y=0.01`로 바닥보다 살짝 띄워 z-fighting 방지

### 4.5 조명 관리 (성능 + 밝기, Phase 4)
- **`ROOM_LIGHT_GROUPS`** — 전시실 인덱스별 그 방 작품 스포트라이트 목록
- **`updateRoomLighting(seg)`** — 매 프레임, 가까운 방(2칸 이내)의 조명만 씬에 추가하고 멀리
  있는 방의 조명은 씬에서 제거(단순 밝기 0보다 훨씬 가벼움). 그림자는 지금 들어와 있는 방에만 켬
- 작품 스포트라이트 그림자맵 해상도: 768×768

### 4.6 데이터 로딩
- **`loadArtworksData()`** — 전시의 모든 전시실·모든 작품을 방 단위로 불러옴.
  `exhibitionInfo`(전시 제목·학년·반)도 함께 담아 로비 사인에 반영
- 데모 모드(`buildDemoArtworks`)도 동일한 방 단위 구조로 반환

### 4.7 QR코드 & 시작 화면 (Phase 5, 신규)
- **`renderEntryQR()`** — 스크립트 로드 시 1회 즉시 실행. `location.origin + location.pathname`
  (지금 페이지의 실제 접속 주소)을 읽어 QRCode.js(cdnjs)로 렌더링. 라이브러리 로드 실패 시
  `#qrEntryBox`에 `.hidden` 클래스를 추가해 조용히 숨김 — throw하지 않고 catch로 흡수해 다른
  기능에 영향 없게 함
- **`#qrCopyLinkBtn` 클릭 핸들러** — `navigator.clipboard.writeText`로 접속 주소 복사, 실패 시
  `window.prompt`로 대체 표시
- URL을 코드에 하드코딩하지 않으므로, 배포 저장소/도메인이 바뀌어도 QR은 항상 정확한 현재 주소를 가리킴

### 4.8 모바일 조작 UI (Phase 6, 전면 재작성)
> **배경**: 기존에는 외부 라이브러리(nipplejs)로 조이스틱을 구현했는데, 이 라이브러리가 CDN
> 문제로 로드에 실패하면 그 지점에서 스크립트 전체가 멈추면서 이후의 `animate()` 렌더 루프
> 시작 코드까지 실행되지 못하는 치명적 문제가 있었다("모바일에서 로비에 진입 후 진행이 안
> 되고, 조이스틱도 안 보이고, 시선 조작법도 알 수 없음" 증상과 정확히 일치). Phase 6에서
> 외부 의존성을 제거하고 자체 구현 + 방어 코드로 전면 교체함.

- **`isTouch`** — `matchMedia("(pointer: coarse) and (hover: none)")`로 터치 기기 여부 판정
- **`joystickVec`, `joyTouchId`, `lookTouchId`** — 이동 벡터, 조이스틱/시선 드래그 각각의 터치
  identifier (한 손가락은 조이스틱, 다른 손가락은 시선 드래그를 동시에 써도 서로 간섭하지 않도록 분리)
- **조이스틱(터치 시작 지점 `#joystickZone`)** — `getBoundingClientRect()` 기반으로 중심을 계산해
  엄지 이동 거리·방향을 `joystickVec.x/y`(-1~1)로 환산. 시각 요소는 `#joystickThumb`을
  `transform: translate()`로 움직이는 방식(CSS 트랜지션 있음)
- **시선 드래그(`renderer.domElement`의 touch 이벤트)** — `inJoyZone(x,y)`로 조이스틱 영역에서
  시작된 터치는 시선 조작에서 제외. 이동이 거의 없었으면(`dragMoved=false`) 탭으로 간주해
  작품 클릭(`pickArtworkAtNDC`) 처리
- **모바일 조작 초기화 코드 전체가 `try/catch`로 감싸져 있음** — 향후 어떤 오류가 나더라도
  나머지 기능(3D 렌더링, 로그인, 관리자 페이지 등)에는 영향이 없도록 방어
- **`renderer.domElement.style.touchAction = "none"`** — 브라우저 기본 스크롤/줌 제스처가 시선
  드래그를 방해하지 않도록 설정
- **`#mobileTutorial` / `mobileTutorialShown`** — 모바일에서 "전시장 입장하기"를 처음 눌렀을
  때 한 번만 "조이스틱으로 이동 / 드래그로 시선 / 탭으로 감상" 안내 카드를 띄움 (`enterBtn`
  클릭 핸들러 안에서 `isTouch && !mobileTutorialShown` 조건으로 트리거)
- (제거됨) **nipplejs CDN 스크립트 태그** — 더 이상 사용하지 않아 `<head>`에서 삭제. 외부
  의존성이 하나 줄어 로드 안정성이 높아짐

### 4.9 감상 결과 리포트 (Phase 6, 신규)
> 관리자 페이지 "기존 전시 관리" 탭에서, 선택한 전시의 좋아요·댓글을 **인터넷 없이도 열리는
> 단일 HTML 파일**로 다운로드하는 기능. 검색·정렬·인쇄까지 파일 안에 내장되어 있어 감상
> 평가 자료로 바로 활용 가능.

- **HTML 진입점**: `#adminMgReportBtn`(다운로드 버튼), `#adminMgReportProgressWrap/Fill/Text`
  (진행률 표시), `#adminMgReportMsg`(결과 메시지) — "작품 목록(전시실별)" 섹션 바로 위에 배치
- **`fetchImageAsDataURL(url)`** — 작품 이미지를 `fetch()`로 내려받아 `<canvas>`로 폭 480px
  내외로 축소 + JPEG 재압축(품질 0.82) 후 데이터 URL로 변환. 리포트 파일 하나의 용량이 너무
  커지지 않게 하기 위함. `fetch`가 실패하면 `null`을 반환하고, 호출부(`generateAppreciationReport`)가
  해당 작품만 원본 원격 URL로 대체 표시(전체 리포트 생성이 중단되지 않도록 개별 이미지 단위로
  degrade)
- **`blobToDataURL(blob)`** — 캔버스가 오염된(taint) 극히 예외적인 경우를 위한 폴백 경로
- **`buildAppreciationReportHtml(data)`** — 실제로 다운로드될 리포트 HTML 문서 전체를 문자열로
  생성하는 템플릿 함수. 요약 통계 카드(전시 작품 수·총 좋아요·총 댓글·좋아요 남긴 방문자 수·
  댓글 작성자 수) + 검색/정렬 컨트롤 + 작품별 카드(썸네일·전시실명·학생 이름·작품 제목·좋아요
  배지·댓글 목록) + 인쇄용 CSS(`@media print`)를 포함. **주의**: 이 함수가 반환하는 문자열
  안에는 `<script>...<\/script>`가 그대로 들어있는데, 닫는 태그를 `<\/script>`로 이스케이프해
  두었다 — 그렇지 않으면 브라우저가 `index.html` 자체의 진짜 `<script>` 블록을 이 지점에서
  끝난 것으로 잘못 해석해 **앱 전체가 깨지는 심각한 버그**가 생긴다. 이 템플릿을 수정할 때는
  반드시 이 이스케이프를 유지할 것
- **`generateAppreciationReport()`** — 버튼 클릭 시 실행되는 메인 함수:
  1. `currentMgExhibitionId` / `mgRooms` / `mgArtRowsForRender`(기존 관리 패널이 이미 불러온
     상태를 재사용) 확인
  2. `likes`/`comments` 테이블을 해당 전시의 작품 id들로 한 번에 조회 (`is_hidden`인 댓글은
     리포트에서도 제외)
  3. 좋아요는 `visitor_key` 기준 `Set`으로 묶어 중복 없이 카운트
  4. 작품마다 순차적으로 `fetchImageAsDataURL` 호출하며 진행률 UI 갱신 (한꺼번에 병렬 요청을
     보내지 않고 순차 처리 — 너무 많은 동시 요청을 피하기 위함)
  5. `buildAppreciationReportHtml`로 문서를 만들고 `Blob` + `<a download>`로 파일 다운로드 트리거
  6. 파일명: `감상결과_{전시제목}_{생성일}.html` (특수문자는 제거)
- 다운로드된 리포트 파일 자체의 내장 JS(검색창 입력, 정렬 셀렉트박스, 인쇄 버튼)는 순수
  vanilla JS로, 외부 라이브러리·네트워크 요청이 전혀 없어 오프라인에서도 완전히 동작함

---

## 5. 배포 관련 핵심 정보 (자세한 절차는 `README.md` 참고)

- **폴더 구조**: 저장소 루트에 `index.html` + `assets/`(배경 이미지 2장)만 있으면 됨
- **GitHub Pages 설정**: Settings → Pages → Deploy from a branch → `main` / `root`
- **Supabase**: 기존 프로젝트를 그대로 사용, anon key는 RLS 전제하에 공개돼도 무방
- **QR 주소**: 배포 후 별도 설정 없이 자동으로 실제 배포 주소를 가리킴 (4.7절 참고)

---

## 6. 그동안 고친 버그들 (전체 누적 이력)

새 대화창에서 비슷한 증상이 다시 나타나면 아래 표를 먼저 참고하세요.

| 증상 | 원인 | 수정 | Phase |
|---|---|---|---|
| 방이 여러 개면 작품이 어둡게 나오고 이름표만 보임 | 방마다 그림자 조명이 있어서, 방 개수가 늘수록 동시에 켜지는 그림자 조명 수가 WebGL 한계를 넘어섬 | `ROOM_LIGHT_GROUPS` + `updateRoomLighting`으로 가까운 방만 조명 활성화 | 4 |
| 방과 방 구분이 안 됨 | 같은 톤·같은 폭으로 일직선 연결 | 문틀, 복도 전용 바닥 톤, 복도 보조등, 방별 포인트 컬러 스트라이프 추가 | 4 |
| 문틀이 너무 낮음 | `DOOR_H=2.6` | `DOOR_H=4.0`으로 확대 | 4 |
| 일직선 동선이라 단조롭고 되돌아오기 번거로움 | 로비→복도→방을 일렬로만 연결 | 로비에 문 2개(A동/B동) + 연결 통로로 ㅁ자 순환 구조 재설계 | 4 |
| 특정 전시실에 아예 입장이 안 됨 | `clampPlayerPosition`이 진행 방향(z)까지 좁게 제한해 문 앞에서 튕겨 나옴 | 세그먼트별로 "진행 방향 축"은 자유롭게, "폭 방향 축"만 제한하도록 재작성 | 4 |
| 문틀이 심하게 떨림(z-fighting) | 문틀과 벽이 거의 같은 깊이에 겹쳐 렌더링되고, 카메라 far(200)가 너무 커서 깊이 정밀도가 낮음 | `logarithmicDepthBuffer:true`, `camera.far` 200→120, 문틀 두께 0.3→0.4 | 4 |
| 연결 통로에서 양옆 전시실 어디로도 못 들어감 | 연결 통로의 앞뒤(z)를 양쪽 다 좁게 제한해서, 전시실로 이어지는 쪽도 막혀버림 | 연결 통로는 막다른 벽 방향만 제한하고 반대쪽(전시실 방향)은 열어둠 + `findSegment`가 통로를 전시실보다 먼저 판정하도록 순서 조정 | 4 |
| 이동 중 화면이 느려짐 | 멀리 있는 조명이 꺼져 있어도(밝기 0) 여전히 씬에 남아 매 프레임 계산됨 + 트인 공간(복도/로비)은 그릴 범위가 넓음 | 멀리 있는 조명을 씬에서 실제로 제거(add/remove) + 그림자맵 해상도 축소 | 4 |
| 로비 사인에 "5학년 3반"이 항상 고정 표시 | 학년/반이 하드코딩돼 있었음 | 실제 전시(`exhibitions` 테이블)의 grade/class를 불러와 표시하도록 변경 | 4 |
| 모바일에서 로비 진입 후 아무 진행이 안 되고, 조이스틱도 안 보이고, 시선 조작법도 알 수 없음 | 조이스틱 라이브러리(nipplejs)가 CDN에서 로드되지 않으면 초기화 코드에서 에러가 나며, 그 뒤에 있던 `animate()` 렌더 루프 시작 코드까지 스크립트 전체가 멈춰버림 | nipplejs 의존성 제거 + 조이스틱을 vanilla JS로 직접 구현, 모바일 초기화 코드 전체를 `try/catch`로 감싸 향후 오류가 나머지 기능을 멈추지 않게 방어 | 6 |
| (수정 과정에서 발견) 감상 결과 리포트 템플릿 문자열 안의 `</script>`가 `index.html`의 실제 스크립트 블록을 조기 종료시켜 페이지 전체가 깨질 뻔함 | HTML 파서는 `<script>` 내용 안에서 JS 문법을 이해하지 못하고, 리터럴 문자열 `</script>`를 그대로 태그의 끝으로 처리함 | 템플릿 문자열 안의 닫는 태그를 `<\/script>`로 이스케이프 (4.9절 참고, 배포 전 자체 문법 검사로 발견 및 수정) | 6 |

---

## 7. 테스트 체크리스트 (누적)

1. 관리자 페이지에서 사진 15장 이상 업로드해 전시실이 3~4개 이상 생기도록 함
2. 로비 왼쪽 문("○○전시실 ↑" 안내판)으로 들어가 첫 방부터 순서대로 관람
3. 전시실 입장/퇴장이 어디서도 막히지 않는지 확인 (특히 연결 통로 앞뒤)
4. 맨 끝 연결 통로를 지나 반대쪽 동으로 넘어가 남은 전시실들을 순서대로 관람
5. 로비 오른쪽 문("출구 ↓" 안내판)으로 다시 돌아오는지 확인
6. 로비 환영 사인에 실제 업로드한 전시의 학년/반이 표시되는지 확인
7. 문틀이 떨리지 않는지, 방마다 다른 색 스트라이프로 구분되는지 확인
8. 작품이 8개 이하(전시실 1개)인 경우에도 정상적으로 동작하는지 확인 (하위 호환)
9. **[Phase 5]** 시작 화면에 QR코드가 정상적으로 그려지는지, 실제 휴대폰으로 스캔해 같은
   전시장 페이지가 열리는지 확인 (PC로 접속한 상태에서 QR을 그려도 스캔은 반드시 다른 기기로 테스트)
10. **[Phase 5]** "🔗 링크 복사" 버튼 클릭 시 클립보드에 정확한 주소가 복사되는지 확인
11. **[Phase 5.5]** 로비 천장-벽 경계에 코브 라이트, 바닥 중앙에 메달리온, 로비 두 출입문에만
    브라스 라인이 보이는지, 전시실 쪽은 이전과 동일하게 차분한 톤인지 확인
12. **[Phase 6]** 모바일 기기(안드로이드·아이폰 모두 가능하면 권장)로 접속해 조이스틱이
    화면 왼쪽 아래에 뚜렷하게 보이는지, 밀었을 때 이동하는지, 화면을 드래그하면 시선이
    움직이는지, 작품을 탭하면 확대되는지 확인
13. **[Phase 6]** 전시장에 처음 입장할 때(모바일) 조작법 안내 카드가 뜨고, "확인했어요"를
    누르면 사라지는지 확인
14. **[Phase 6]** 관리자 페이지 "기존 전시 관리"에서 "📊 감상 결과 리포트 다운로드" 클릭 →
    파일이 다운로드되는지, 그 파일을 **인터넷을 끄고** 열어도 이미지·좋아요·댓글이 모두
    보이는지, 검색·정렬·인쇄 버튼이 동작하는지 확인

---

## 8. 다음 단계 로드맵

**Phase 1~6 (완료)**
전시 자동 생성 · 1인칭 관람 · 좋아요/댓글 · 관리자 페이지 · 다중 전시실 & 순환 동선 · QR코드 &
배포 · 로비 디자인 개선 · 모바일 UX 안정화 · 감상 결과 리포트

**보류 중 (다시 논의 필요 시 참고)**
- **성능 옵션(지오메트리 병합)** — 방별 디자인을 다르게 하고 싶다는 요구와 충돌 가능성이 있어
  보류. 방별 디자인 커스터마이징을 먼저 설계/구현한 뒤, 그 구조(재질 종류 수·텍스처 차이
  정도)가 확정되면 그에 맞는 병합 전략(재질별 그룹 병합 / vertex color)을 선택하는 순서를 권장
- **댓글 부적절어 필터** — 사용자가 명시적으로 나중으로 미룸

**Phase 7 — 다음 작업 후보**
- 전시실별 디자인 커스터마이징 (색·질감·소품을 방마다 다르게)
- 부적절어 필터 (보류 해제 시)
- 성능 옵션(보류 해제 시, 방별 디자인 구조 확정 후 재검토)
- 실사용 피드백 반영
